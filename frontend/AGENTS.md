# Persistent Codex instructions

- Before starting any code task in this workspace, check the working tree status and pull the latest changes from the remote repository.
- Use a safe sequence: inspect local changes first; if the working tree is clean, run `git pull`; if there are uncommitted changes, ownership errors, network failures, or conflicts, stop and report the issue instead of overwriting local work.
