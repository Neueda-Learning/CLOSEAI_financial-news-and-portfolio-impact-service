import { DashboardPage } from './DashboardPage'

/** A contained alternate visual direction; the warm product theme is unchanged. */
export function ForestThemePreviewPage() {
  return <DashboardPage themeVariant="forest" />
}
